type User = { email: Text }

type List Text -> Text
func joinEmails = items=>    "joined"

value users : List User = [
    { email: "ada@example.com" }
]

value joinedEmails = users
 *|> .email
 <|* .missing
