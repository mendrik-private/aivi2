type User = { email: Text }

fun joinEmails:Text items:List Text =>
    "joined"

value users:List User = [
    { email: "ada@example.com" }
]

value joinedEmails =
    users
     *|> .email
     <|* .missing
