domain Duration over Int
    suffix ms : Int = value => Duration value
    (+) : Duration -> Duration -> Duration
    toInt : Duration -> Int
    toInt = duration => duration.carrier

value total : Duration = 10ms + 5ms
value raw : Int = toInt total
