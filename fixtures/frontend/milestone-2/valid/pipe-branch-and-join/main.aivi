type User = { email: Text }

value collect items =>
    items

value ready = True
value start = "start"
value wait = "wait"
value branch =
    ready
     T|> start
     F|> wait

value users = []
value emails =
    users
     *|> .email
     <|* collect
