domain Duration over Int
    suffix sec : Int = value => Duration value

domain Retry over Int
    suffix times : Int = value => Retry value

func step = x =>
    x

@recur.timer 5sec
signal polled : Signal Int = 0
 @|> step
 <|@ step

@recur.backoff 3times
value retried : Task Int Int = 0
 @|> step
 <|@ step
