type Screen =
  | Loading
  | Ready Text
  | Failed Text

value current: Screen = Loading

value boolLabel:Text ready:Bool =>
    ready
     ||> True  => "ready"
     ||> False => "waiting"

value maybeLabel:Text maybeUser:(Option Text) =>
    maybeUser
     ||> Some name => name
     ||> None      => "login"

value resultLabel:Text status:(Result Text Text) =>
    status
     ||> Ok body     => body
     ||> Err message => message

value validationLabel:Text status:(Validation Text Text) =>
    status
     ||> Valid body      => body
     ||> Invalid message => message

value screenLabel:Text screen:Screen =>
    screen
     ||> Loading       => "loading"
     ||> Ready title   => title
     ||> Failed reason => reason

value nestedScreenLabel:Text screen:Screen =>
    screen
     ||> Loading       => "loading"
     ||> Ready title   => (title == "Inbox" T|> "priority" F|> title)
     ||> Failed reason => reason

value screenView =
    <with value={current} as={screen}>
        <match on={screen}>
            <case pattern={Loading}>
                <Label text="Loading..." />
            </case>
            <case pattern={Ready title}>
                <Label text={title} />
            </case>
            <case pattern={Failed reason}>
                <Label text={reason} />
            </case>
        </match>
    </with>
