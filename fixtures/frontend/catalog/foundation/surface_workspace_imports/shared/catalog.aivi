type Text -> Text
func greetUser = name=>    "hello {name}"

type Text -> Text -> Text
func joinLabels = left right=>    "{left} / {right}"

value defaultRole : Text = "owner"

export greetUser
export joinLabels
export defaultRole
