val name = "Ada"
val count = 3
val title = "Inbox"
val status = "ready"

val greeting = "Hello {name}, you have {count} items"
val statusLine = "{title} ({status})"

val datePattern = rx"\d{4}-\d{2}-\d{2}"
val slugPattern = rx"[a-z0-9]+(-[a-z0-9]+)*"
