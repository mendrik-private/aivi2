use aivi.network (
    http
    socket
)

type Bool = True | False

type Vec2 =
  | Vec2 Int Int

type Result A = Err Text | Ok A

value answer = 42

type Int -> Int -> Int
func add = x y=>    x + y

type Int -> Result Int
func defaultResult = x=>    Ok x

signal counter = 0
signal nextCounter = counter + 1
