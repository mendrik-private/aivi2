use aivi.network (
    http
    socket
)

type Bool = True | False

type Vec2 = Vec2 Int Int

type Result A = Err Text | Ok A

value answer = 42

value add:Int x:Int y:Int =>
    x + y

value defaultResult:Result Int x:Int =>
    Ok x

signal counter = 0
signal nextCounter = counter + 1
