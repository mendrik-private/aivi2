use aivi.duration (
  Duration
)
use aivi.http (
  HttpError
  Timeout
  DecodeFailure
  RequestFailure
  HttpHeaders
  HttpQuery
  HttpResponse
  HttpTask
  DecodeMode
  Strict
  Retry
)
use aivi.timer (
  TimerTick
  TimerReady
)
use aivi.log (
  LogLevel
  Debug
  Error
  LogContext
  LogEntry
  LogError
  LogTask
  LogWrite
)
use aivi.fs (
  FsError
  NotFound
  PermissionDenied
  ReadFailed
  WriteFailed
  FsEvent
  Created
  Changed
  Deleted
  writeText
  writeBytes
  createDirAll
  deleteFile
)
use aivi.stdio (
  stdoutWrite
  stderrWrite
)
use aivi.auth (
  PkceConfig
  PkceToken
  PkceError
  UserCancelled
  PkceTimeout
)
use aivi.dbus (
  DbusValue
  DbusString
  DbusInt
  DbusBool
  DbusCall
  DbusSignal
  BusNameState
  Owned
  Queued
  Lost
  DbusError
  NoReply
)
use aivi.gnome.onlineAccounts (
  GoaAccount
  GoaAccountId
  GoaCapability
  Mail
  GoaProvider
  GoaError
  AccessToken
  OAuthToken
)
use aivi.gnome.notifications (
  Notification
  NotificationAction
  NotificationError
  NotificationFailed
)
use aivi.db (
  DbError
  SchemaMismatch
  QueryFailed
  SortDir
  Asc
  Desc
)
use aivi.imap (
  SyncState
  ImapError
  ImapAuthFailed
  ImapConnectionFailed
)
use aivi.smtp (
  SmtpConfig
  SmtpMessage
  SmtpError
  SmtpAuthFailed
  Attachment
)
use aivi.url (
  Url
  UrlError
)
use aivi.path (
  Path
  PathError
)

type User = {
  id: Int
  name: Text
}

val headers: HttpHeaders = Map {
  "Authorization": "Bearer demo"
}

val query: HttpQuery = Map {
  "page": "1"
}

val decodeMode: DecodeMode = Strict

type RetryBudget = Retry

type UsersResponse = HttpResponse (List User)
type UsersTask     = HttpTask (List User)

@source http.get "https://api.example.com/users"
sig users: Signal UsersResponse

@source timer.every 120 with {
  immediate: True
  coalesce: True
}
sig tick: Signal TimerTick

@source timer.after 1000
sig ready: Signal TimerReady

val timeoutError: HttpError = Timeout

val decodeError: HttpError = DecodeFailure "bad-json"

val requestError: HttpError = RequestFailure "offline"

val level: LogLevel = Debug

val context: LogContext = Map {
  "module": "stdlib"
}

val entry: LogEntry = {
  level: level,
  message: "loaded",
  context: context
}

type Writer          = LogWrite
type CurrentLogTask  = LogTask
type CurrentLogError = LogError

type PollDelay = Duration

val errorLevel: LogLevel = Error

val fsCreated: FsEvent = Created
val fsChanged: FsEvent = Changed
val fsDeleted: FsEvent = Deleted

val fsNotFound: FsError = NotFound "/etc/missing"
val fsPermDenied: FsError = PermissionDenied "/root/file"

val pkceUserCancelled: PkceError = UserCancelled
val pkceTimedOut: PkceError = PkceTimeout

val busOwned: BusNameState = Owned
val busQueued: BusNameState = Queued
val busLost: BusNameState = Lost
val dbusNoReply: DbusError = NoReply

val testCall: DbusCall = {
  destination: "org.gnome.AiviMail.Daemon",
  path: "/org/gnome/AiviMail/Daemon",
  interface: "org.gnome.AiviMail.IDaemon",
  member: "TriggerSync",
  body: []
}

val testSignal: DbusSignal = {
  path: "/org/gnome/AiviMail/Daemon",
  interface: "org.gnome.AiviMail.IDaemon",
  member: "SyncStateChanged",
  body: []
}

type MailCapability = GoaCapability

val mailCap: GoaCapability = Mail

val notification: Notification = {
  summary: "New mail",
  body: Some "You have 3 new messages",
  icon: Some "mail-unread",
  actions: []
}

val notifFailed: NotificationError = NotificationFailed "dbus error"

val schemaMismatch: DbError = SchemaMismatch "v2 required"
val queryFailed: DbError = QueryFailed "no such table"
val sortAsc: SortDir = Asc
val sortDesc: SortDir = Desc

val syncState: SyncState = {
  lastSyncedAt: None,
  inProgress: True,
  error: None
}

val imapAuthFailed: ImapError = ImapAuthFailed
val imapConnFailed: ImapError = ImapConnectionFailed "timeout"

val smtpConfig: SmtpConfig = {
  host: "smtp.gmail.com",
  port: 587
}

val smtpAuthFailed: SmtpError = SmtpAuthFailed

type CliWrite = Text -> Task Text Unit
type CliWriteBytes = Text -> Bytes -> Task Text Unit

val stdoutWriter: CliWrite = stdoutWrite
val stderrWriter: CliWrite = stderrWrite
val fsWriteText: Text -> Text -> Task Text Unit = writeText
val fsWriteBytes: CliWriteBytes = writeBytes
val fsCreateDirAll: CliWrite = createDirAll
val fsDeleteFile: CliWrite = deleteFile

val stdoutTask: Task Text Unit = stdoutWrite "stdout"
val stderrTask: Task Text Unit = stderrWrite "stderr"
val mkdirTask: Task Text Unit = createDirAll "/tmp/aivi-boundary"
val writeTextTask: Task Text Unit = writeText "/tmp/aivi-boundary/out.txt" "ok"
val deleteTask: Task Text Unit = deleteFile "/tmp/aivi-boundary/out.txt"

@source process.args
sig cliArgs: Signal (List Text)

@source process.cwd
sig cliCwd: Signal Text

@source env.get "ACCESS_TOKEN"
sig cliToken: Signal (Option Text)

@source stdio.read
sig stdinText: Signal Text

@source path.home
sig homeDir: Signal Text

@source path.configHome
sig configHomeDir: Signal Text

@source path.dataHome
sig dataHomeDir: Signal Text

@source path.cacheHome
sig cacheHomeDir: Signal Text

@source path.tempDir
sig tempDir: Signal Text
