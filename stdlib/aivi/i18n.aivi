use aivi.i18n (
    tr
    trn
)

type Text -> Text
func translate = t =>
    tr t

type Text -> Text -> Int -> Text
func translatePlural = singular plural count =>
    trn singular plural count

export (tr, trn, translate, translatePlural)
