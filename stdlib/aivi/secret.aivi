type SecretError =
  | SecretUnavailable Text
  | SecretLocked
  | SecretCancelled
  | SecretProtocolError Text

type SecretSource = Unit

type SecretTask A = (Task SecretError A)

export (SecretError, SecretUnavailable, SecretLocked, SecretCancelled, SecretProtocolError, SecretSource, SecretTask)
