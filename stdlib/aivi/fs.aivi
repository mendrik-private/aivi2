hoist

type FsError =
  | NotFound Text
  | PermissionDenied Text
  | ReadFailed Text
  | WriteFailed Text
  | FsProtocolError Text

type FsEvent =
  | Created
  | Changed
  | Deleted

type FsSource = Unit

export (
    FsError, NotFound, PermissionDenied, ReadFailed, WriteFailed, FsProtocolError,
    FsSource,
    FsEvent, Created, Changed, Deleted
)
