type EnvSource = Unit
type EnvEntry = (Text, Text)
type EnvLookupTask = (Task Text (Option Text))
type EnvListTask = (Task Text (List EnvEntry))

export (
    EnvSource
    EnvEntry
    EnvLookupTask
    EnvListTask
)
