hoist

use aivi.nonEmpty (
    NonEmptyList
    singleton
    cons
    head
    toList
    last
    length
    mapNel
    fromList
    appendNel
    fromHeadTail
    init
)

export NonEmptyList
export singleton
export cons
export head
export last
export length
export toList
export mapNel
export fromList
export appendNel
export fromHeadTail
export init
