value http : Text = "http"
value socket : Text = "socket"
value mailbox : Text = "mailbox"

type Request A = A

type Channel A B = { left: A }

export http
export socket
export Request
export Channel
